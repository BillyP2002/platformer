using UnityEngine;

public class QBlockAnimate : MonoBehaviour
{
    public GameObject qBlock;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
